# Default: auto CRLF for text files on Windows
* text=auto

# Shell scripts MUST stay LF (will fail on Linux if CRLF)
*.sh text eol=lf

# Terraform files: LF
*.tf text eol=lf
*.tfvars text eol=lf

# Python: auto
*.py text=auto

# YAML: LF
*.yml text eol=lf
*.yaml text eol=lf

# HTML / CSS: auto
*.html text=auto
*.css text=auto

# Binary files
*.png binary
*.jpg binary
*.db binary
